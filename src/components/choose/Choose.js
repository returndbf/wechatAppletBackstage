export class Choose{
    id;
    prefix;
    content;
    delFlag;

    constructor(id, prefix, content, delFlag) {
        this.id = id;
        this.prefix = prefix;
        this.content = content;
        this.delFlag = delFlag;
    }
}